services:
  homeeconomics:
    image: ghcr.io/jordidiaz/homeeconomics:latest
    pull_policy: always
    container_name: homeeconomics
    ports:
      - "6001:6001"
    env_file:
      - .env
    restart: unless-stopped
